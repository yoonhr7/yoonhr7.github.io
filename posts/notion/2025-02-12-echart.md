---
title: "Echart"
date: "2025-02-12"
tags: ["Vue","Library"]
notionId: "198a784e-4dc2-808f-b8de-ebde1a974e8e"
lastEditedTime: "2026-01-10T04:21:00.000Z"
---
<aside data-icon="💡">

Vue3를 기준으로 작성된 글입니다.

</aside>


[bookmark](https://echarts.apache.org/examples/en/index.html#chart-type-bar)


### 설치


```powershell
npm install echarts
```


### component


```javascript
<template>
    <div ref="chartContainer"></div>
</template>

<script setup>
import { ref, onMounted, onBeforeUnmount, watch } from 'vue';
import * as echarts from 'echarts';

// Props 정의
const props = defineProps({
    option: Object
});

const chartContainer = ref(null);
let chartInstance = null;

// 차트 초기화 함수
const initChart = () => {
    if (chartContainer.value) {
        chartInstance = echarts.init(chartContainer.value);
        chartInstance.setOption(props.option);
    }
};

onMounted(() => {
    initChart();

    // ResizeObserver를 사용하여 컨테이너 크기 변화를 감지하고 차트 리사이즈
    const resizeObserver = new ResizeObserver(() => {
        if (chartInstance) chartInstance.resize();
    });
    resizeObserver.observe(chartContainer.value);

    // 창 크기 변경 시 차트 리사이즈
    const resizeCharts = () => {
        if (chartInstance) chartInstance.resize();
    };
    window.addEventListener('resize', resizeCharts);

    // Cleanup
    onBeforeUnmount(() => {
        resizeObserver.disconnect();
        window.removeEventListener('resize', resizeCharts);
        if (chartInstance) chartInstance.dispose();
    });
});

// 차트 옵션이 변경될 때마다 업데이트
watch(
    () => props.option,
    newOption => {
        if (chartInstance) {
            chartInstance.setOption(newOption);
        }
    },
    { deep: true }
);
</script>
```


### main.js에 전역으로 등록


```javascript
import ECharts from './components/ECharts.vue'

import { use } from "echarts/core"
import { CanvasRenderer } from "echarts/renderers"
import { BarChart } from "echarts/charts"
import { GridComponent, TooltipComponent, TitleComponent } from "echarts/components"

app.component('ECharts', ECharts) // 전역 컴포넌트로 등록
```


### EChart 사용


```html
<ECharts :option="chartOption" class="w100p h256px"></ECharts>
```


```javascript
const chartOption = {
    title: { show: false },
    grid: { top: 16, left: 24, right: 8, bottom: 28 },
    tooltip: {},
    xAxis: {
        axisLine: { show: false },
        axisTick: { show: false },
        nameGap: 4,
        nameLocation: 'center',
        nameTextStyle: { color: '#666' },
        axisLabel: { interval: 'auto' },
        data: ['23년 7월', '23년 8월', '23년 9월', '23년 10월', '23년 11월', '23년 12월', '24년 1월', '24년 2월', '24년 3월', '24년 4월']
    },
    yAxis: {
        splitLine: { lineStyle: { color: ['#EAEAEA'], type: 'dashed', dashOffset: 2 } },
        nameTextStyle: { color: '#666' }
    },
    series: [
        {
            name: '월별 매출액',
            type: 'bar',
            barWidth: 16,
            itemStyle: {
                borderRadius: [16, 16, 0, 0],
                color: {
                    type: 'linear',
                    x: 0,
                    y: 0,
                    x2: 0,
                    y2: 1,
                    colorStops: [
                        { offset: 0, color: '#3AC9A5' },
                        { offset: 1, color: 'rgba(165, 219, 13, 0.50)' }
                    ]
                }
            },
            data: [0.3, 0.4, 0.6, 1.1, 1.9, 2.6, 3.6, 4.2, 5, 5.7]
        }
    ]
};
```

