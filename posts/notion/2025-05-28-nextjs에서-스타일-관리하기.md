---
title: "Next.js에서 스타일 관리하기"
date: "2025-05-28"
tags: ["React","css"]
notionId: "201a784e-4dc2-805a-9fcc-ca87ceadba2b"
lastEditedTime: "2026-01-10T04:21:00.000Z"
---
## 1. 전역으로 관리 (일반적인 경우)


## 2. 컴포넌트(페이지)별로 관리

- vue 의 `scoped`와 비슷하게 관리하기 위해 Next.js에서 사용하는 방법들
<details>
<summary>scoped란?</summary>

```javascript
<style scoped>
  .my-div {
    color: red;
  }
</style>
```


이렇게 `<style scoped>`라고 쓰면, 이 스타일은 **해당 컴포넌트 내부에서만** 적용된다. 즉, 다른 컴포넌트에는 영향을 주지 않는다.




</details>


| 방식                        | 스타일 범위           | 사용법                    | 장점            | 단점                   |
| ------------------------- | ---------------- | ---------------------- | ------------- | -------------------- |
| **CSS Modules**           | 컴포넌트 단위 (scoped) | `Component.module.css` | 간단하고 공식 지원    | 동적 스타일링 어려움          |
| **Styled JSX**            | 컴포넌트 단위 (scoped) | `<style jsx>`          | Next.js 기본 내장 | JS 표현이 제한됨           |
| ~~**Styled Components**~~ | 컴포넌트 단위 (scoped) | `styled.div``...       | 강력한 동적 스타일    | 설정 필요 (babel-plugin) |
| ~~**Tailwind CSS**~~      | 유틸리티 클래스 기반      | 클래스명에 따라 작동            | 빠른 스타일링       | 클래스 길어짐              |
| ~~**Global CSS**~~        | 전역               | `globals.css` 등        | 공통 스타일 관리     | 범위 제한 어려움            |


### 1. **CSS Modules**

- Next.js 공식 권장 방식 중 하나

```typescript
// Button.module.css
.button {
  background-color: royalblue;
  color: white;
}
```


```typescript
// Button.tsx
import styles from './Button.module.css';

export default function Button() {
  return <button className={styles.button}>Click me</button>;
}
```


### 2. **Styled JSX** (Next.js 기본 내장)

- **Next.js가 자동으로 클래스명을 해시해서 붙이는 방식으로 scope를 흉내**내는 방식이다.

```typescript
export default function Button() {
  return (
    <><button>Click me</button>
      <style jsx>{`
        button {
          background: royalblue;
          color: white;
        }
      `}</style>
    </>
  );
}
```


🔽실제 브라우저 렌더링 결과


```javascript
<button class="jsx-3e0a1a0d202f3482">Click me</button>
<style id="__jsx-3e0a1a0d202f3482">
  .jsx-3e0a1a0d202f3482 button {
    background: royalblue;
  }
</style>
```


<aside data-icon="💡">

Vue의 <style scoped>와 유사한 개념

- Vue는 `data-v-xxxx` 속성을 DOM과 CSS에 추가함
- Styled JSX는 `.jsx-xxxx` 클래스를 자동 추가함

</aside>

