---
title: "GSAP 꿀팁 🍯"
date: "2025-05-29"
tags: ["JavaScript","animation","Library","GSAP"]
notionId: "202a784e-4dc2-805d-9c1b-d10fdda09b5b"
lastEditedTime: "2026-01-10T04:21:00.000Z"
---
### 🐝 GSAP 애니메이션 중 display 속성이 inline-block으로 바뀌는 현상

- GSAP는 transform 애니메이션 시작할 때 내부적으로 `display: inline-block` 세팅하는데, 이걸 방지하려면 **`gsap.set`****으로 미리 원하는 display 스타일을 강제 지정**하고 시작하면 됩니다.

    ```javascript
    gsap.set(".letter", { display: "flex" })
    
    gsap.to(".letter", {
      duration: 1,
      yPercent: 100,
      ease: "expo.inOut",
      repeat: -1,
      yoyo: true,
      repeatDelay: 2,
      stagger: {
        each: 0.04,
        from: "random",
      },
    })
    ```


### 🐝 GSAP 3.13.x 버전부터 모든 기능이 무료로 변경됨

- 3.13.x 이상 버전으로 업데이트하면 모든 기능을 무료로 사용 가능해집니다.

    [bookmark](https://gsap.com/pricing/)


    [bookmark](https://gsap.com/docs/v3/Installation/)


    ```shell
    npm install gsap
    ```


    ```shell
    yarn add gsap
    ```

