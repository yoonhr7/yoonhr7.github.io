---
title: "three.js"
date: "2025-02-26"
description: "Vue3 + Nuxt3 환경에서 three.js를 이용해 3D애니메이션 구현하기"
tags: ["Library","Vue"]
notionId: "1a6a784e-4dc2-8077-ae6e-fdb574cb6ba6"
lastEditedTime: "2026-01-10T04:21:00.000Z"
---
## 1. 시작하기


### 1) npm 설치


```powershell
npm install three @types/three
```


### 2) Nuxt 환경에서 Three.js 시작하기


1️⃣ Nuxt 플러그인으로 Three.js설정 (선택사항)


`three.js`는 클라이언트에서만 실행되므로, 플러그인으로 등록하면 SSR 관련 오류를 방지할 수 있음


```typescript
// plugins/three.client.ts
export default defineNuxtPlugin(() => {
  return {
    provide: {
      three: () => import('three'),
    },
  };
});
```




2️⃣ glb 모델 불러오기


```typescript
<script setup lang="ts">
import { ref, onMounted, onBeforeUnmount } from "vue";

const modelContainer = ref<HTMLElement | null>(null);
const isVisible = ref(false);

const { $three, $gltfLoader } = useNuxtApp();
let scene: any, camera: any, renderer: any;

onMounted(async () => {
    if (!process.client) return; // 서버 사이드에서 실행 방지

    scene = new $three.Scene();
    camera = new $three.PerspectiveCamera(45, window.innerWidth / window.innerHeight, 0.1, 100);
    camera.position.set(0, 1, 3);

    renderer = new $three.WebGLRenderer({ alpha: true, antialias: true });
    renderer.setSize(window.innerWidth, window.innerHeight);
    renderer.setPixelRatio(window.devicePixelRatio);
    modelContainer.value?.appendChild(renderer.domElement);

    await loadModel();
    animate();
});

const loadModel = async () => {
    const gltf = await $gltfLoader.loadAsync("/models/OakleyConfigV20.glb");
    if (!gltf) return;

    gltf.scene.scale.set(10, 10, 10); 
    gltf.scene.position.set(0, 1, 0);
    scene.add(gltf.scene);
};

const animate = () => {
    requestAnimationFrame(animate);
    renderer.render(scene, camera);
};

onBeforeUnmount(() => {
    renderer.dispose();
});
</script>

<template>
    <div ref="modelContainer" class="three-container"></div>
</template>
```




### 2) 3D 모델 선택 (GLB, GLTF, FBX, OBJ)


🡆 **GLTF vs GLB 선택 기준 (**Three.js 및 최신 3D 툴에서 표준이 됨.)

- `.glb` : 파일 하나로 관리하고 싶을 때 (GLTF의 바이너리 버전, 텍스처와 애니메이션까지 한 파일에 포함)
- `.gltf` : 텍스처 등을 따로 관리하고 싶을 때
- **GLB > GLTF > FBX > OBJ** 순으로 추천!

| **포맷**          | **GLB** (바이너리 GLTF)                 | **GLTF** (JSON 기반)               | **FBX**                 | **OBJ**                     |
| --------------- | ----------------------------------- | -------------------------------- | ----------------------- | --------------------------- |
| **파일 확장자**      | `.glb`                              | `.gltf`                          | `.fbx`                  | `.obj` (+ `.mtl` 필요)        |
| **파일 크기**       | 🔥 가장 작음 (Draco 압축됨)                | 작지만 텍스처 파일이 따로 존재                | 큼                       | 큼                           |
| **로드 속도**       | 🔥 가장 빠름                            | 빠름                               | 느림                      | 느림                          |
| **텍스처 포함**      | ✅ 포함 (단일 파일)                        | ❌ 별도 관리 (`.bin`, `.jpg` 등)       | ✅ 포함 가능하지만 별도 관리될 수도 있음 | ❌ `.mtl` 파일로 관리             |
| **애니메이션 지원**    | ✅ 지원 (최적화됨)                         | ✅ 지원                             | ✅ 지원                    | ❌ 없음                        |
| **머티리얼 유형**     | PBR (Physically Based Rendering)    | PBR (Physically Based Rendering) | 일반적인 머티리얼 (PBR 지원 X)    | 기본 머티리얼 (`.mtl` 필요)         |
| **Three.js 지원** | ✅ `GLTFLoader`                      | ✅ `GLTFLoader`                   | ✅ `FBXLoader` (느릴 수 있음) | ✅ `OBJLoader` + `MTLLoader` |
| **웹 최적화**       | ✅ 최고 (WebGL 최적화)                    | ✅ 좋음                             | ❌ 최적화 부족                | ❌ 최적화 부족                    |
| **압축 가능 여부**    | ✅ Draco 압축 가능                       | ✅ 가능                             | ❌ 압축 기능 없음              | ❌ 압축 없음                     |
| **사용 사례**       | 🔥 웹(WebGL), 게임, AR/VR, 애니메이션 포함 모델 | 웹(WebGL), 게임, 별도 파일 관리 가능할 때     | 오래된 3D 툴과의 호환성          | 단순한 정적 모델                   |


### 3) 배경 이미지 (HDR, EXR, JPG/PNG)


| 포맷          | 색상 깊이        | 동적 범위 | 로더              | 장점                       | 단점       |
| ----------- | ------------ | ----- | --------------- | ------------------------ | -------- |
| **HDR**     | 32-bit float | 높음    | `RGBELoader`    | 용량이 EXR보다 작고, 고퀄리티 표현 가능 | 정보 손실 가능 |
| **EXR**     | 32-bit float | 매우 높음 | `EXRLoader`     | 고퀄리티, 깊이 정보 저장 가능        | 파일 크기 큼  |
| **JPG/PNG** | 8-bit        | 낮음    | `TextureLoader` | 파일 크기 작음, 간단한 사용         | 동적 범위 부족 |


💡 **HDR, EXR을 사용할 때** **`PMREMGenerator`****를 사용하면 더 좋은 조명 효과를 얻을 수 있음.**


```javascript
const pmremGenerator = new THREE.PMREMGenerator(renderer);
const envMap = pmremGenerator.fromEquirectangular(texture).texture;
scene.environment = envMap;
texture.dispose(); // 원본 텍스처 정리
```

- **HDR 불러오기**

    ```typescript
    import * as THREE from "three";
    import { RGBELoader } from "three/examples/jsm/loaders/RGBELoader.js"; // HDR 로더
    const loadHDR = async () => {
        const rgbeLoader = new RGBELoader();
        const pmremGenerator = new THREE.PMREMGenerator(renderer);
        pmremGenerator.compileEquirectangularShader();
    
        rgbeLoader.load("/textures/image.hdr", (texture) => {// 이미지 경로
            const envMap = pmremGenerator.fromEquirectangular(texture).texture;
            scene.environment = envMap; // PBR 환경 조명 적용
            scene.background = null; // 배경을 HDR로 설정: envMap / 배경 투명 : null
            texture.dispose();
            pmremGenerator.dispose();
        });
    };
    ```

- **EXR 불러오기**

    ```typescript
    import * as THREE from "three";
    import { EXRLoader } from "three/examples/jsm/loaders/EXRLoader.js"; // EXR 로더
    const loadEXR = async () => {
        const exrLoader = new EXRLoader();
        const pmremGenerator = new THREE.PMREMGenerator(renderer);
        pmremGenerator.compileEquirectangularShader();
    
        exrLoader.load(
            "/textures/image.exr", // 이미지 경로
            (texture) => {
                texture.mapping = THREE.EquirectangularReflectionMapping; // 환경 매핑 적용
                const envMap = pmremGenerator.fromEquirectangular(texture).texture;
    
                scene.environment = envMap; // PBR 환경 조명 적용
                scene.background = null; // 배경을 EXR로 설정 : envMap / 배경 투명 : null
    
                texture.dispose();
                pmremGenerator.dispose();
            },
            undefined,
            (error) => {
                console.error("EXR 파일 로드 실패:", error); // 디버깅용 콘솔 로그
            }
        );
    };
    ```


### 4) Three.js 주요 Matertial 종류


| 원하는 효과                       | 추천 머터리얼                |
| ---------------------------- | ---------------------- |
| **그냥 텍스처만 보이게 (조명 영향 X)**    | `MeshBasicMaterial`    |
| **거칠기, 반사 조절 가능 (현실적인 재질)**  | `MeshStandardMaterial` |
| **고급 재질 (유리, 투명, 굴절 효과 포함)** | `MeshPhysicalMaterial` |
| **확산광 효과 (성능 최적화)**          | `MeshLambertMaterial`  |
| **광택 있는 표면 (shininess 조절)**  | `MeshPhongMaterial`    |
| **만화 스타일 (셀 셰이딩)**           | `MeshToonMaterial`     |


| 머터리얼                     | 조명 영향        | 설명                                                              |
| ------------------------ | ------------ | --------------------------------------------------------------- |
| **MeshBasicMaterial**    | ❌ (조명 영향 없음) | 텍스처나 색상을 그대로 보여줌, 그림자/반사 없음                                     |
| **MeshStandardMaterial** | ✅ (조명 영향 받음) | 현실적인 거칠기(`roughness`), 금속 느낌(`metalness`) 지원                    |
| **MeshPhysicalMaterial** | ✅ (조명 영향 받음) | `MeshStandardMaterial` + 투명도(`transmission`), 굴절(`ior`) 등 고급 효과 |
| **MeshLambertMaterial**  | ✅ (조명 영향 받음) | 부드러운 확산광 (퍼지는 빛) 효과, 성능이 가벼움                                    |
| **MeshPhongMaterial**    | ✅ (조명 영향 받음) | `shininess`(광택) 지원, 거칠기 조절 불가능                                  |
| **MeshToonMaterial**     | ✅ (조명 영향 받음) | 셀 셰이딩(만화 느낌) 효과                                                 |
| **MeshDepthMaterial**    | ❌ (조명 영향 없음) | 깊이 값을 기반으로 렌더링 (깊이 맵 생성에 사용)                                    |
| **MeshNormalMaterial**   | ❌ (조명 영향 없음) | 법선(normal) 방향을 RGB 색상으로 표현                                      |


---


## 2. 최적화


| 방법                      | 효과                    |
| ----------------------- | --------------------- |
| **Draco 압축**            | GLB 파일 크기 감소 (최대 90%) |
| **KTX2 텍스처 압축**         | 로딩 속도 개선              |
| **IndexedDB 캐싱**        | 동일 파일 재요청 방지          |
| **Web Worker + DRACO**  | CPU 부하 감소             |
| **CDN 사용**              | 네트워크 속도 최적화           |
| **Lazy Load + Preload** | 렌더링 최적화               |


### **1) Draco 압축과 KTX2(BasisU) 압축**


| 압축 방식                         | 적용 대상                                    | 특징                                 |
| ----------------------------- | ---------------------------------------- | ---------------------------------- |
| **Draco Compression**         | **메시 데이터 (Vertex, Normal, UV 등)**        | 파일 크기 감소, 로딩 속도 향상 (but 실시간 해제 필요) |
| **KTX2 (BasisU) Compression** | **텍스처 (이미지 데이터: Diffuse, Normal Map 등)** | GPU에서 직접 디코딩 가능, 성능 최적화            |


**Draco는 3D 모델의 기하학적 데이터를 압축**하고, **KTX2는 텍스처 이미지를 압축**하는 방식입니다.


**KTX2 압축 후 Draco 압축하는 순서가 권장됩니다.**


## Draco & KTX2(BasisU)압축하는 방법

## Draco 압축


|            | 압축 전 (glTF)   | 압축 후 (Draco glTF)       |
| ---------- | ------------- | ----------------------- |
| **파일 크기**  | 10MB          | 2MB (~80% 감소)           |
| **로딩 속도**  | 느림            | 빠름                      |
| **지원 포맷**  | `.gltf, .glb` | `.glb` (Draco 압축)       |
| **디코딩 속도** | 빠름            | 약간 느림 (WASM 사용 시 매우 빠름) |


1️⃣ **CMD(명령 프롬프트) 실행**

- `윈도우 키 + R` → `cmd` 입력 → `Enter`
- 또는 **PowerShell**(`Win + X` → `Windows PowerShell` 선택) 사용 가능

2️⃣ **Node.js 설치 (만약 없으면)**

- [Node.js 공식 사이트](https://nodejs.org/)에서 LTS 버전 다운로드 후 설치
- 설치 후 CMD에서 `node -v` 입력 → 버전이 나오면 정상 설치됨
- `npm -v` 입력 → 버전이 나오면 npm도 정상 설치됨

3️⃣ **Draco 및 KTX2 압축 프로그램 설치**

- **draco 설치 (gltf-pipeline 설치)**

    ```shell
    npm install -g gltf-pipeline
    ```


    _(👉_ _`-g`_ _옵션은 전역(global) 설치 옵션, 한 번만 설치하면 됨.)_

- **ktx2 설치**

    ```shell
    npm install -g @gltf-transform/cli
    ```


    아래 링크에서 최신 버전의 `KTX-Software` 다운로드
    ▶ [**KTX-Software Releases**](https://github.com/KhronosGroup/KTX-Software/releases)

    - 설치 및 버전 확인

    ```shell
    ktx --version
    ```

    - 설치가 안되었다면 환경변수 path에 직접 추가하기

    <aside data-icon="👉">
    
    PATH에 추가하기
    
    - **Windows + R** → `sysdm.cpl` 입력 후 실행
    - `고급` 탭 → `환경 변수(N)...` 클릭
    - `시스템 변수(S)`에서 **Path**를 찾아 선택 → `편집(E)...` 클릭
    - `새로 만들기(N)` 버튼 클릭
    - `C:\Program Files\KTX-Software\bin` 입력 후 **확인**
    - 모든 창을 닫고 **컴퓨터를 재부팅하거나 로그아웃 후 다시 로그인**
    
    </aside>


4️⃣ **Draco 및  KTX 압축 적용**

- **해당 폴더에서 CMD 실행**
    - `Shift + 마우스 오른쪽 버튼` → **"여기에서 PowerShell 열기"** 또는 **"여기에서 명령 창 열기"** 클릭
    - 또는 `cmd` 창에서 `cd` 명령어로 해당 폴더로 이동

        ```shell
        cd C:\Users\사용자이름\Desktop\GLTF_Models
        ```


<aside data-icon="💡">

권장순서 : KTX2 (BasisU) 텍스처 압축 → Draco 압축

</aside>

- **KTX2 (BasisU) 텍스처 압축 적용**

    ```plain text
    gltf-transform etc1s model.glb model_ktx2.glb
    ```

    - 입력 파일: `model.glb`
    - 출력 파일: `model_ktx2.glb`
    - 같은 폴더에 `model_ktx2.glb`가 생성됨
- **Draco 압축 적용**

    ```plain text
    gltf-pipeline -i model_ktx2.glb -o model_final.glb -d
    ```

    - 입력 파일: `model_ktx2.glb`
    - 출력 파일: `model_final.glb`
    - 같은 폴더에 `model_final.glb`가 생성됨

<aside data-icon="⚠️">

Warning: Decoded KHR_draco_mesh_compression. Further compression will be lossy.


→ 이미 `Draco`가 적용된 `GLB`를 다시 압축하면 **손실(Lossy)**이 발생할 수 있다는 경고입니다.

</aside>

- 압축결과

![image.png](https://prod-files-secure.s3.us-west-2.amazonaws.com/65a7a4f3-5212-474e-9cb7-2dc73b477f61/f2a2c5ea-7e2c-4644-9d1e-7b901c34e11e/image.png?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&X-Amz-Credential=ASIAZI2LB466TWYDCVT2%2F20260110%2Fus-west-2%2Fs3%2Faws4_request&X-Amz-Date=20260110T082028Z&X-Amz-Expires=3600&X-Amz-Security-Token=IQoJb3JpZ2luX2VjEPD%2F%2F%2F%2F%2F%2F%2F%2F%2F%2FwEaCXVzLXdlc3QtMiJGMEQCIG%2B5dBzcOsT6cYls1hdS5sHTUnLX5uIVBn5RPr14mBdyAiAh5uT0SQRce20hy7410qhJvRYJtpehZfaFNokA6fpjUSqIBAi5%2F%2F%2F%2F%2F%2F%2F%2F%2F%2F8BEAAaDDYzNzQyMzE4MzgwNSIMVpuENpabSXF1akrMKtwDEMY%2FlBZC%2FmYfJszUvomMOqjZ%2BSt5whpT9zWmJsclZKVKRmfPJVK8tMg4ZpD2pByPd0sANnKzZGCBcOZ8qt6bFx9izji%2FmkGPMDwfMUqE4wy4ne3gk%2FiFJiEHJKo4SO7MnCCUTdX2RKIjvLgbBUu1WvweQG%2BNCazH7O%2FM6JDcttIjZIHOqUF%2FTFJ%2FT7c5RjQs8NtSXFxZNCeWOyJcgqrG7s5V6DQixfcx7A%2FCOwmVlzUo2z5lAJIwrXxMKBS7WqBQgMwu%2FOUiVKe2Mb2kMo%2BMoKpWQX96O6FySERqjOZ1I6mKDFip90i0BiAj16b4opIkrI%2FoY9N7uxHocNR%2BYL6Sb5aigRNNA%2BUSRmlpzqWZhUEB0ulno122s78N6LHHywVkFx1qcBxq3EFL4poZkrO91ZDytVRVjtl5R1c4WfC3NJGyIFnTtyie%2BeMaGxLnUjKLF5OU3Hfhe7qKhZS4VxkH2s0o2iTn8aIW2juzAYEYoml6Rw0pnou4FnqNL2FposXEVg9aCfmkLi2BXNLgVhgfXRh5ELh%2B81gV%2BQnG6xcNOUHdOlKRFqTjx1TKsMlK2O8a9R1dp0yd3bOBdnauhuwNHzV89G1xmQewKgGuaIPzRjOqJsMDK2ATZK5950cwqI2IywY6pgGseM7xyaR%2FTEgTKbKX0szAf1FCeDy4aMqYgwlq8EHGrzqLM6CVz21SM6DuKKlhGYPp6dg9miqFFQqK4Wj1uRgYDDbvUnSpJAoFbEidvobSlb%2BcjfJ81uLu6HdMYI52g%2Fm7XHW5RjBaiJmQQD6xiwKEheg%2BkfQcWCKQLpKcaHypRSQyNAgU77AJSsmm118EG9kCK7ip2XHqqqf%2BrpqEfaB1lY%2F5YymY&X-Amz-Signature=c0f313c1caeb760414fedde15eb9c14f55ce3fbcb10333fd82a2a5e43e653a24&X-Amz-SignedHeaders=host&x-amz-checksum-mode=ENABLED&x-id=GetObject)


---


## Draco 디코딩


### 1) cdn 이용하기


```typescript
import { GLTFLoader } from "three/examples/jsm/loaders/GLTFLoader";
import { DRACOLoader } from "three/examples/jsm/loaders/DRACOLoader";

const loader = new GLTFLoader();

const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath("https://www.gstatic.com/draco/v1/decoders/");
loader.setDRACOLoader(dracoLoader);
```


### 2) 파일 다운

- 아래의 링크에서 파일을 다운받습니다.

    [https://github.com/mrdoob/three.js/tree/dev/examples/jsm/libs/draco](https://github.com/mrdoob/three.js/tree/dev/examples/jsm/libs/draco) 


    [Draco_decoder.zip](https://prod-files-secure.s3.us-west-2.amazonaws.com/65a7a4f3-5212-474e-9cb7-2dc73b477f61/623d8480-018e-46c7-8b34-84a22d8522b1/Draco_decoder.zip?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&X-Amz-Credential=ASIAZI2LB466YGQPXSYH%2F20260110%2Fus-west-2%2Fs3%2Faws4_request&X-Amz-Date=20260110T082034Z&X-Amz-Expires=3600&X-Amz-Security-Token=IQoJb3JpZ2luX2VjEPD%2F%2F%2F%2F%2F%2F%2F%2F%2F%2FwEaCXVzLXdlc3QtMiJHMEUCIQD%2B50mx%2BvAmFAQbhawSwWLJ%2Fpv7waHpgDyYiuPvQk8TJQIgahsh%2FOllA5%2BDJ8TaVeF8XJkGt6cZlYx1LDNKYTbgHAYqiAQIuf%2F%2F%2F%2F%2F%2F%2F%2F%2F%2FARAAGgw2Mzc0MjMxODM4MDUiDK3qABIqQQsgiqTOdyrcA0OuxoXheKQ4gsfJMd6hg5MU4aVWCmpCMi%2F1OaQzRaqraA99RugRZLIvEkBqevTDflhH2wQXjCdhd3EbeuJz6IjdAZr3ThzuN6stijJVNmx7duYovjOUjcqoiRR8FKwfMuB4ZxpE45gvwrkLfF8fyMKt%2BPghNs5sSjes1b5Sdb2iO62yF36LLsKdFkJaM4ib%2FjY6%2FPkvMsvFs7W6UQUYvcL6x77fXu452y69Ie3%2BsUMd2QizDl%2FSUI7TZhxzjK934gfzhJnvro%2B0n1KzU0NW3MmJzbcQAqGVFejF3p9sjvC9bcM3mfGFhQq4UP3ek5b7hiK7T4NC%2FXwcSlvo6oMpSZNUlpi7gfo9LZH3%2FlbDgIogg9UgQDDiztWaWGHYSIQ54NBavaAAVx9TyVIugQxqx9Vq8d8lhuIXAd3UyUd71RHBB6VjEy70A%2BIGsYOCH1CRNfOjNzTlEukEaY8M6L2s041lNojzNQUWXrBKG1Is0T05m9jMh%2FOq5FYdR8fePrkzvSmCc19ny4o7oOtoq%2B6qQ4Z4LvHej%2BP4CuUIPBhqzYxWhsMqgcsk%2FzEKp%2B%2BSiqsHQHkbNaufRKMuVNmyPMfupuEavzJP3k9FCMaPeljhXTVdwvKG1kSl9RCTx2sbMO2NiMsGOqUBc619G%2Bgyr84JKBIZbP2eRpdDHeVF5V6EwvPhQ6HU0P%2FeCgdCrdJAhyMv0fkXNekZE9u4zUW34sR%2BucYK%2FZyPkUeTUzSce21vpo1V%2BiqCM%2B9j22Fzd43Xg2QfkyEl72cw0z3W%2BrJ8ordGm01PQGt0VYCDYoFzYQwdNnzai0tzkOJRqY%2Fhih8R6wHPybQ3JrSnk5cpkPRLya9v3D3e4G%2BIsefVynuR&X-Amz-Signature=eafbc1e54c848cb3408db24263e40e41c7c5de60cdc356788ae249b617a30563&X-Amz-SignedHeaders=host&x-amz-checksum-mode=ENABLED&x-id=GetObject)


    ```typescript
    📁 프로젝트 루트
    ├── 📁 public
    │   ├── 📁 draco
    │   │   ├── draco_decoder.js
    │   │   ├── draco_decoder.wasm
    │   │   ├── draco_wasm_wrapper.js
    │   │   ├── draco_decoder_worker.js
    ```


    | 파일 이름                     | 설명                          | JavaScript / WASM |
    | ------------------------- | --------------------------- | ----------------- |
    | `draco_decoder.js`        | JavaScript 버전 디코더 (속도 느림)   | JS                |
    | `draco_decoder.wasm`      | WebAssembly 버전 디코더 (속도 빠름)  | WASM              |
    | `draco_wasm_wrapper.js`   | WASM 파일을 실행하는 JavaScript 코드 | WASM              |
    | `draco_decoder_worker.js` | Web Worker를 사용한 멀티스레드 디코딩   | JS                |

- 불러오기

    ```typescript
    import { GLTFLoader } from "three/examples/jsm/loaders/GLTFLoader";
    import { DRACOLoader } from "three/examples/jsm/loaders/DRACOLoader";
    
    const loader = new GLTFLoader();
    
    const dracoLoader = new DRACOLoader();
    dracoLoader.setDecoderPath("/draco/");
    loader.setDRACOLoader(dracoLoader);
    ```

- 추가 확인 사항
    - `draco_wasm_wrapper.js`와 `draco_decoder.wasm` 파일이 **정상적으로 서버에서 서빙**되고 있는지 확인하세요.
    - 만약 WASM 파일을 직접 지정해야 하는 경우(환경에 따라) 다음과 같이 `setDecoderConfig`를 설정할 수도 있습니다.하지만 일반적으로 `setDecoderPath`만 잘 지정해주면 자동으로 wasm을 사용하거나, wasm을 지원하지 않을 경우 JS fallback으로 동작합니다.

        ```typescript
        dracoLoader.setDecoderConfig({ type: 'wasm' });
        ```


## KTX2 디코딩 (basis)

- 아래의 링크에서 파일을 다운받습니다.

    [https://github.com/mrdoob/three.js/tree/dev/examples/jsm/libs/basis](https://github.com/mrdoob/three.js/tree/dev/examples/jsm/libs/basis)


    ```typescript
    📁 프로젝트 루트
    ├── 📁 public
    │   ├── 📁 basis
    │   │   ├── basis_transcoder.js
    │   │   ├── basis_transcoder.wasm
    ```

- 불러오기

    ```typescript
    import { GLTFLoader } from "three/examples/jsm/loaders/GLTFLoader";
    import { KTX2Loader } from "three/examples/jsm/loaders/KTX2Loader";
    
    const loader = new GLTFLoader();
    
    const ktx2Loader = new KTX2Loader();
    ktx2Loader.setTranscoderPath("/basis/"); // basis 디코더 경로 설정 (경로 확인 필요)
    ktx2Loader.detectSupport(renderer); // renderer를 통해 지원 확인
    loader.setKTX2Loader(ktx2Loader);
    ```

- **압축된 파일 불러오기**

    Draco  압축된 GLB 파일을 불러오기 위해 `GLTFLoader`와 `DRACOLoader`를 함께 사용합니다.


    ```typescript
    import { onMounted } from 'vue'
    import * as THREE from 'three'
    import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader.js'
    import { DRACOLoader } from 'three/examples/jsm/loaders/DRACOLoader.js'
    
    onMounted(() => {
      const scene = new THREE.Scene()
      const renderer = new THREE.WebGLRenderer({ antialias: true })
      renderer.setSize(window.innerWidth, window.innerHeight)
      document.body.appendChild(renderer.domElement)
    
      const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000)
      camera.position.set(0, 1, 3)
    
      const loader = new GLTFLoader()
      const dracoLoader = new DRACOLoader()
      dracoLoader.setDecoderPath('https://www.gstatic.com/draco/v1/decoders/') // CDN 사용으로 속도 향상
      loader.setDRACOLoader(dracoLoader)
    
      loader.load('/models/compressed_model.glb', (gltf) => {
        scene.add(gltf.scene)
        animate()
      })
    
      function animate() {
        requestAnimationFrame(animate)
        renderer.render(scene, camera)
      }
    })
    ```


### 2) LOD(Level of Detail)


<aside data-icon="💡">

LOD(Level of Detail)란 멀리 있는 객체의 디테일을 줄이고 가까운 객체는 높은 디테일로 유지하는 기법입니다. 이 기법은 렌더링 성능을 최적화하고, GPU 부하를 줄이며, 프레임 속도를 향상 시키는 데 중요한 역할을 합니다.

</aside>

- **GPU & CPU 성능 최적화**
    - **멀리 있는 객체의 삼각형(폴리곤) 개수를 줄여 렌더링 성능 향상**.
    - LOD가 없으면 모든 오브젝트가 최대 디테일로 렌더링되므로 GPU 부하 증가.
    - **LOD를 적용하면 멀리 있는 객체는 저해상도로 렌더링하여 GPU 부담 감소**.
- **메모리 사용 최적화**
    - LOD를 사용하면 **불필요한 고해상도 데이터를 메모리에 유지할 필요 없음**.
    - VRAM 및 CPU 메모리 사용량을 줄여 더 많은 객체를 관리할 수 있음.
- **렌더링 속도 향상**
    - 낮은 디테일의 모델을 렌더링하면 **드로우 콜(Draw Call) 수 감소**.
    - 프레임 속도(FPS)가 향상되며, **대규모 씬에서도 부드러운 렌더링 가능**.
- **게임 및 실시간 렌더링에 유용**
    - 게임 엔진(예: Unity, Unreal Engine)에서도 LOD를 활용하여 **멀리 있는 오브젝트를 저해상도로 표현**.
    - Three.js에서도 **복잡한 3D 모델을 렌더링할 때 성능 최적화에 필수**.

**1. GLTF 모델에 LOD 적용**


```typescript
const loader = new GLTFLoader();

loader.load("/models/high.glb", (gltf) => {
    const highDetailModel = gltf.scene;
    highDetailModel.scale.set(5, 5, 5);

    loader.load("/models/medium.glb", (gltf) => {
        const midDetailModel = gltf.scene;
        midDetailModel.scale.set(5, 5, 5);

        loader.load("/models/low.glb", (gltf) => {
            const lowDetailModel = gltf.scene;
            lowDetailModel.scale.set(5, 5, 5);

            // ✅ LOD 설정
            const lod = new THREE.LOD();
            lod.addLevel(highDetailModel, 0);
            lod.addLevel(midDetailModel, 10);
            lod.addLevel(lowDetailModel, 30);

            scene.add(lod);
        });
    });
});
```


✔ **거리에 따라 자동으로** **`high`****,** **`mid`****,** **`low`** **모델을 전환**하여 최적화 가능.


**2. 카메라 거리 기반 LOD 적용**


```typescript
function animate() {
    requestAnimationFrame(animate);
    lod.update(camera); // ✅ 카메라 거리 기반 LOD 업데이트
    renderer.render(scene, camera);
}
animate();
```


**🎯 LOD 적용 결과**


| **거리(m)** | **렌더링 모델** | **렌더링 속도(FPS)** |
| --------- | ---------- | --------------- |
| 0 ~ 10    | 고해상도 모델    | FPS 낮음 (고퀄리티)   |
| 10 ~ 30   | 중해상도 모델    | FPS 중간 (최적화됨)   |
| 30+       | 저해상도 모델    | FPS 높음 (성능 향상)  |


**🚀 기대 효과**


✅ **멀리 있는 오브젝트는 저해상도로 렌더링하여 성능 최적화**


✅ **카메라가 가까워질수록 고해상도 모델로 전환하여 디테일 유지**


✅ **Three.js에서 수백~수천 개의 모델을 렌더링할 때 유용**


---


## 3. 기능


### 1) GLB 불러오기 + `OrbitControls` (vue3)


```javascript
<template>
    <div ref="threeContainer" class="three-container"></div>
</template>

<script setup>
import { onMounted, ref } from "vue";
import * as THREE from "three";
import { GLTFLoader } from "three/examples/jsm/loaders/GLTFLoader.js";
import { OrbitControls } from "three/examples/jsm/controls/OrbitControls.js"; // ✅ 추가

const threeContainer = ref(null);

onMounted(() => {
    // 1️⃣ 씬, 카메라, 렌더러 설정
    const scene = new THREE.Scene();
    scene.background = new THREE.Color(0xeeeeee); // 회색 배경
    const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
    camera.position.set(0, 2, 5);

    const renderer = new THREE.WebGLRenderer({ antialias: true });
    renderer.setSize(window.innerWidth, window.innerHeight);
    renderer.setPixelRatio(window.devicePixelRatio);

    renderer.outputEncoding = THREE.sRGBEncoding;
    renderer.toneMapping = THREE.ACESFilmicToneMapping; // 톤 매핑 추가하면 더 자연스러움
    renderer.toneMappingExposure = 1.2; // 밝기 조정

    threeContainer.value.appendChild(renderer.domElement);

    // 2️⃣ 조명 추가 (쉐이딩을 위해 필수)
    const ambientLight = new THREE.AmbientLight(0xffffff, 2); // 부드러운 환경광
    scene.add(ambientLight);

    const directionalLight = new THREE.DirectionalLight(0xffffff, 3);
    directionalLight.position.set(-5, 5, 5);
    scene.add(directionalLight);

    const pointLight = new THREE.PointLight(0xffffff, 2, 100); // 강한 점광원
    pointLight.position.set(-2, 3, 2);
    scene.add(pointLight);

    // 3️⃣ OrbitControls 추가 (마우스로 회전 가능)
    const controls = new OrbitControls(camera, renderer.domElement);
    controls.enableDamping = true; // 부드러운 회전 효과
    controls.dampingFactor = 0.05;
    controls.screenSpacePanning = false;
    controls.minDistance = 2; // 최소 줌 거리
    controls.maxDistance = 10; // 최대 줌 거리
    controls.maxPolarAngle = Math.PI / 2; // 위쪽 회전 제한

    // 4️⃣ GLTFLoader를 사용하여 GLB 모델 로드
    const loader = new GLTFLoader();
    loader.load(
        "/models/testModel.glb",
        (gltf) => {
            const model = gltf.scene;
            model.position.set(0, 0, 0); // 모델 위치 조정
            model.scale.set(20, 20, 20);

            // 모델의 모든 Mesh에 대해 쉐이딩 적용
            model.traverse((child) => {
                if (child.isMesh && child.material) {
                    const oldColor = child.material.color.clone(); // 원본 색상 저장
                    child.material = new THREE.MeshPhongMaterial({
                        color: oldColor, // 기존 색상 유지
                        shininess: 50, // 광택 조정 (Phong 전용)
                        specular: 0x111111, // 하이라이트 컬러 (Phong 전용)
                    });
                    child.material.color.convertSRGBToLinear(); // ✅ 색상 보정 적용
                    child.material.needsUpdate = true;
                }
            });

            scene.add(model);
        },
        undefined,
        (error) => {
            console.error("GLB 모델 로드 실패:", error);
        }
    );

    // 4️⃣ 애니메이션 루프 (OrbitControls 업데이트 추가)
    function animate() {
        requestAnimationFrame(animate);
        controls.update(); // ✅ OrbitControls 업데이트
        renderer.render(scene, camera);
    }
    animate();

    // 5️⃣ 창 크기 변경 대응
    window.addEventListener("resize", () => {
        camera.aspect = window.innerWidth / window.innerHeight;
        camera.updateProjectionMatrix();
        renderer.setSize(window.innerWidth, window.innerHeight);
    });
});
</script>

<style scoped>
.three-container {
    width: 100%;
    height: 100vh;
    overflow: hidden;
}
</style>
```


### 2) 마우스를 이용한 rotation, zoom 컨트롤 (OrbitControls)

- three.js에서 제공되는 `OrbitControls`를 활용합니다.

```typescript
import { OrbitControls } from 'https://cdn.jsdelivr.net/npm/three@0.155/examples/jsm/controls/OrbitControls.js'

const controls = new OrbitControls(camera, renderer.domElement)
controls.enableDamping = true // 부드러운 감속 효과
controls.dampingFactor = 0.05
controls.screenSpacePanning = false
controls.minDistance = 1
controls.maxDistance = 10
controls.maxPolarAngle = Math.PI // 위아래 회전 제한 (90도)

function animate() {
  requestAnimationFrame(animate)
  controls.update() // 컨트롤 업데이트 필요
  renderer.render(scene, camera)
}
animate()
```


### 3) 반응형 (resize)


```typescript
window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight
  camera.updateProjectionMatrix()
  renderer.setSize(window.innerWidth, window.innerHeight)
})
```


### 4) 섹션마다 모델에 애니메이션 추가하기 (스크롤에 따라)


```typescript
// gsap 애니메이션
    const sections = document.querySelectorAll(".section");

    $gsap.to(gltf.rotation, {
        y: "+=" + Math.PI * (3 / 4), // 목표 회전값
        scrollTrigger: {
            trigger: sections[0],
            start: "top top",
            end: "bottom top",
            scrub: true,
        },
    });
    $gsap.to(gltf.scale, {
        x: scaleFactor.value,
        y: scaleFactor.value,
        z: scaleFactor.value,
        scrollTrigger: {
            trigger: sections[1],
            start: "top top",
            end: "bottom top",
            scrub: true,
        },
    });
    $gsap.to(gltf.position, {
        x: window.innerWidth * -0.0002, // 화면 가로 크기에 1% 비례
        y: window.innerHeight * -0.0002, // 화면 세로 크기에 2% 비례
        scrollTrigger: {
            trigger: sections[1],
            start: "top top",
            end: "bottom top",
            scrub: true,
        },
    });
```


```typescript
<template>
	<div class="section-container">
    <section class="section">section1</section>
    <section class="section">section2</section>
    <section class="section">section3</section>
  </div>
</template>
```

