---
title: "Git"
date: "2025-02-17"
tags: ["협업"]
notionId: "19da784e-4dc2-8032-aadd-cdef02604c72"
lastEditedTime: "2026-01-10T04:21:00.000Z"
---
## Git 마지막 푸시 되돌리기


### 1. **`git reset`** **(강제로 푸시된 커밋 삭제)**


`git reset`을 사용하면 **커밋 이력**을 되돌릴 수 있습니다. **푸시된 커밋을 완전히 삭제**하고, 로컬 상태를 이전 커밋으로 되돌릴 수 있습니다.


a. git reset --hard (마지막 푸시한 커밋 삭제 및 이전 상태로 완전히 되돌리기)


이 방법은 **마지막 커밋을 삭제**하고, **로컬 브랜치를 이전 커밋 상태로 완전히 되돌리는 방법**입니다. 푸시한 커밋이 없었던 것처럼 되돌릴 수 있습니다.


```shell
git reset --hard HEAD~1
```

- `HEAD~1`: 마지막 커밋을 가리킵니다. 이 명령은 **현재 브랜치에서 한 커밋 전으로 되돌리는 명령**입니다.
- `-hard`: 로컬에서 변경된 사항도 모두 되돌립니다 (스테이징된 파일, 워킹 디렉터리까지 포함).



b. git push --force (강제로 푸시하여 원격 리포지토리에도 적용)


위의 `reset`으로 로컬에서는 이전 커밋 상태로 되돌리게 되었지만, 원격 리포지토리에도 이 변경을 반영하려면 **강제 푸시**가 필요합니다.


```shell
git push --force
```


**주의:** `--force`를 사용하면 원격 리포지토리에서 커밋 히스토리가 변경되므로, 팀원들이 같은 리포지토리를 작업 중이라면 이로 인해 문제가 발생할 수 있습니다.




### 2. **`git revert`** **(새로운 커밋으로 되돌리기)**


`git revert`는 커밋을 **되돌리는 새로운 커밋**을 만듭니다. 이 방법은 **이력을 그대로 두고, 변경 사항만 되돌리기** 때문에 안전합니다. **하지만, 커밋을 "삭제"하는 것이 아니라, 취소하는 형태**입니다.


```shell
git revert HEAD
```

- `HEAD`: 가장 최근 커밋을 대상으로 합니다.
- 이 명령어는 **새로운 커밋**을 만들어서 마지막 커밋을 되돌립니다.

그 후, 되돌린 커밋을 푸시하면 됩니다:


```shell
git push
```


### 3. **`git reset`** **후** **`git push --force-with-lease`** **(강제로 푸시하면서 안전하게)**


`git push --force`는 강제로 푸시를 하기 때문에 **다른 사람의 작업에 영향을 미칠 수 있습니다**. **`--force-with-lease`** 옵션은 **자신의 작업만 덮어쓰고, 다른 사람의 작업은 덮어쓰지 않도록** 보호하는 방법입니다.


```shell
git push --force-with-lease
```


이 방법은 **`--force`**보다는 안전한 방법이지만, 여전히 원격 리포지토리에서 커밋 이력을 변경하게 되므로 팀원들과 협의 후 사용하는 것이 좋습니다.


요약

- **마지막 푸시를 삭제하고 이전 상태로 되돌리려면:**
    - `git reset --hard HEAD~1`으로 로컬에서 커밋을 되돌리고,
    - `git push --force`로 원격 리포지토리에 강제로 푸시합니다.
- **커밋을 삭제하는 대신 취소하는 방법은:**
    - `git revert HEAD`로 새로운 커밋을 만들어 되돌립니다.
- **팀 프로젝트에서 푸시된 커밋을 강제로 삭제해야 할 경우:**
    - `git push --force-with-lease`를 사용하는 것이 더 안전합니다.

---


## Git 사용자 설정


### 1. 전역(글로벌)로 사용자 설정하기


```shell
git config --global user.name
git config --global user.email
```


### 2. 특정 리포지토리에서만 사용자명 변경하기


```shell
git config user.name "Your GitHub Username"
git config user.email "your_email@github.com"
```


---


## SSH


### **1. 공개 키 확인하는 방법 (Mac/Linux/WSL/Git Bash)**

<details>
<summary>공개키와 개인키의 개념</summary>
1. **공개키(Public Key):**

    공개키는 이름 그대로 **공개된 키**입니다. 이를 통해 **암호화된 메시지를 복호화**하거나 **사용자를 인증**하는 데 사용됩니다. 공개키는 여러 사람과 공유될 수 있으며, 보통 서버에 저장되거나 서비스의 **SSH 설정**에 추가됩니다.

2. **개인키(Private Key):**

    개인키는 해당 키쌍에서 **비밀로 유지되는 키**입니다. 공개키와 짝을 이루며, 개인키를 소유한 사람만 공개키로 암호화된 데이터를 **복호화**할 수 있습니다. 개인키는 절대로 공개되지 않고, 소유자만 알게 됩니다.


</details>

1. **터미널 열기**
    - **Mac**: `⌘ + Space` → "터미널" 검색 후 실행
    - **Linux**: `Ctrl + Alt + T`
    - **Windows (Git Bash 사용)**: Git Bash 실행
    - **Windows (WSL 사용)**: WSL(우분투) 실행
2. **다음 명령어 입력**

    ```shell
    cat ~/.ssh/id_rsa.pub
    ```


    🔹 **설명:** `cat` 명령어는 파일 내용을 출력해주는 역할을 합니다.


    🔹 **경로:** `~/.ssh/id_rsa.pub` → SSH 공개 키 파일 위치

3. **출력된 결과 확인**
    - 정상적인 공개 키는 다음과 같은 형식이어야 합니다.

        ```shell
        ssh-rsa AAAAB3NzaC1yc2EAAAABIwAAAQEA7... your_email@example.com
        ```

    - `ssh-rsa`, `ecdsa-sha2-nistp256`, `ed25519` 등으로 시작해야 합니다.
    - 이걸 **GitHub/GitLab에 등록하면 됩니다**!

### **2. SSH 키가 없다고 나오는 경우**


❌ `No such file or directory` 오류가 뜬다면, **SSH 키가 아직 생성되지 않은 것**입니다.


### 3. SSH 키 새로 생성하는 방법

1. **새로운 SSH 키 생성 (****`id_rsa`****)**

    ```shell
    ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
    ```

    - 프롬프트가 뜨면 그냥 `Enter`(기본 경로 사용)
    - 패스워드는 필요하면 입력, 아니면 그냥 `Enter`
2. **다시 확인**

    ```shell
    cat ~/.ssh/id_rsa.pub
    ```


    이제 공개 키가 정상적으로 생성된 걸 확인할 수 있습니다! 🎉


### **4. 복사해서 GitHub/GitLab에 등록하기**

1. `cat ~/.ssh/id_rsa.pub` 실행
2. 출력된 키를 **전체 복사** (`ssh-rsa`부터 끝까지)
3. GitHub → **Settings → SSH and GPG keys → New SSH Key**
4. 복사한 키를 붙여넣고 저장

    ![image.png](https://prod-files-secure.s3.us-west-2.amazonaws.com/65a7a4f3-5212-474e-9cb7-2dc73b477f61/cf443010-b3ec-4482-8f1a-d16db9c9223a/image.png?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&X-Amz-Credential=ASIAZI2LB4665HRULNP4%2F20260110%2Fus-west-2%2Fs3%2Faws4_request&X-Amz-Date=20260110T082056Z&X-Amz-Expires=3600&X-Amz-Security-Token=IQoJb3JpZ2luX2VjEPD%2F%2F%2F%2F%2F%2F%2F%2F%2F%2FwEaCXVzLXdlc3QtMiJHMEUCIQDyrT503s7aKYOvrOcPfsLqYUd9NK3zFH3e5VRc0J%2B2rQIgVT6mYCHlarbGJW6sNlXvLhhKGYvDCyPr0OFFRUyr5rsqiAQIuf%2F%2F%2F%2F%2F%2F%2F%2F%2F%2FARAAGgw2Mzc0MjMxODM4MDUiDIXxpBMxQLcJiN2aWircAzJnNcIGX3%2FNpVaaJmNBSVuJw9jZHk66qFTo0tUMlozBnVdDMujoRRBRySoxqqvFW7Gz%2BqMdS3EjcSTBZeRgCYxVCjKyguxmsQrNMAuItWYIRCMJXnjNhbe1hPi4mEEywEggdYL9YsD2lMrj%2FFzMJS2acvzPT1i%2FXo6qcch%2FrcaQNDJHw%2B%2BCQeV3XQWnIuYe6CsHNcXQuAf%2B7Z%2BeRYaLdamF0H%2BN6APmUt%2FB9w%2BYaiEsN5dDQkiYtO9pIQbKTUG631XTGfg9R%2FuQUuDrxxGl0IbSh2%2BhZSBgN2om7bm%2B6A4KzOr4zLK7mg7U8zaDocFsNNfv2FDQrhl8fKascm0MOEcHCOf8NQrkr%2F0HeYLma6cCVYQlg1RIsrU6aQw4DkLQ8%2FJLoCyP9fYI6I9S%2FdVd1WWwb7KlAg6RKEnOSrnYa9u4dn%2BCQ1in5Ev9HTEXJhCS4s41V26L4HONH0BPRSmhDgAKyJ2coh5pa2RsQU93jUYZJc%2Bk10dj%2FEVgk28M3Rj8hn7lp5l8YSGh8qSeCLF%2FxjBAyINT9%2By7X1qc%2FhnIWtDlQXiINBLuTH%2BBlbZZVoaBwb0qNb3IMPKlaEgof%2F7cmUWCx6rOEfFya3AtCOHpuQdndegXt8AOFBI2vqeiMJeNiMsGOqUBI7N%2BAuKHIijDqq8i4T%2Bj1God%2BN%2BgFeU4Fvx7dvGe%2F3chgvzgUxiTJ%2FY8lV6q1MUHx2elnEIbqNk9z%2BzEhUO%2B1Qhpi1AYOKxeh%2FK5%2BvFLwLdKrN9%2FSti%2BjB6907YEboGYQJzbHr3pWcGZIs5fVMrcaLqc8CRNtXLokdB1gdpz3URnIBsfwcp6ZCDlUrv2ATaJtelxYyzuhPkTb3iJGTZ9bpxKEl3n&X-Amz-Signature=5cb03754eb556a32f30c695f363683ad0861dd0a14cc2fcc016a29a84279aa8d&X-Amz-SignedHeaders=host&x-amz-checksum-mode=ENABLED&x-id=GetObject)

5. **테스트:**`"Hi username! You've successfully authenticated"` 메시지가 나오면 성공! 🎉

    ```shell
    ssh -T git@github.com
    ```


**추가 꿀팁**


🔹 **Windows 사용자는 Git Bash에서 실행하면 문제 없이 작동**


🔹 **WSL(우분투) 사용자도 같은 방법으로 실행 가능**


🔹 **Mac/Linux도 동일한 방법 적용 가능**


---

